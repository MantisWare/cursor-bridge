{
  "name": "cursor-bridge",
  "version": "1.0.1",
  "description": "Cursor Bridge - AI-powered browser debugging and automation tools",
  "private": true,
  "workspaces": [
    "cursor-bridge-extension",
    "cursor-bridge-mcp",
    "cursor-bridge-server"
  ],
  "scripts": {
    "build:extension": "npm run build:extension:zip",
    "build:extension:zip": "./scripts/build-extension.sh",
    "build:all": "npm run build:extension && npm run build:mcp && npm run build:server",
    "build:mcp": "cd cursor-bridge-mcp && npm run build",
    "build:server": "cd cursor-bridge-server && npm run build",
    "install:all": "npm install && npm run install:mcp && npm run install:server",
    "install:mcp": "cd cursor-bridge-mcp && npm install",
    "install:server": "cd cursor-bridge-server && npm install",
    "start:mcp": "cd cursor-bridge-mcp && npm start",
    "start:server": "cd cursor-bridge-server && npm start",
    "clean": "rm -rf dist && rm -rf cursor-bridge-extension/dist && rm -rf cursor-bridge-mcp/dist && rm -rf cursor-bridge-server/dist"
  },
  "keywords": [
    "cursor",
    "bridge",
    "chrome-extension",
    "mcp",
    "browser-automation",
    "ai",
    "debugging",
    "development-tools"
  ],
  "author": "Waldo Marais (MantisWare)",
  "license": "MIT",
  "dependencies": {
    "ajv": "^6.12.6"
  }
}
